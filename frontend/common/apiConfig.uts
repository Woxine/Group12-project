export const BACKEND_BASE_URL = 'http://192.168.31.246:8080'

export const buildApiUrl = (path: string): string => {
	if (path.startsWith('/')) {
		return `${BACKEND_BASE_URL}${path}`
	}
	return `${BACKEND_BASE_URL}/${path}`
}
