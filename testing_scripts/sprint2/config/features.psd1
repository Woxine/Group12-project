@{
    Features = @{
        id2 = @(
            "*PaymentCard*Test"
        )
        id3 = @(
            "*Security*Test",
            "*Auth*Test"
        )
        id7 = @(
            "*Email*Test",
            "*Booking*Email*Test"
        )
        id10_11 = @(
            "*BookingExtension*Test",
            "*ExtendBooking*Test"
        )
        all = @(
            "*PaymentCard*Test",
            "*Security*Test",
            "*Auth*Test",
            "*Email*Test",
            "*Booking*Email*Test",
            "*BookingExtension*Test",
            "*ExtendBooking*Test"
        )
    }
}
