@{
    Features = @{
        id9_guest_booking = @(
            "*GuestBooking*Test",
            "*SalespersonBindCard*Test"
        )
        id22_discount = @(
            "*Discount*Test"
        )
        id23_concurrency = @(
            "*Concurrent*Test"
        )
        all = @(
            "*GuestBooking*Test",
            "*SalespersonBindCard*Test",
            "*Discount*Test",
            "*Concurrent*Test"
        )
    }
}
