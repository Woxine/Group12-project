spring:
  application:
    name: backend

  # --- Database Configuration ---
  # Shared defaults for team development.
  # Keep secrets out of Git and provide them via environment variables
  # or an ignored application-local.yaml with the "local" profile enabled.
  datasource:
    # The agreed local database name is scooter_db.
    # Override with SPRING_DATASOURCE_URL when a teammate uses another host or port.
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/scooter_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    driver-class-name: ${SPRING_DATASOURCE_DRIVER_CLASS_NAME:com.mysql.cj.jdbc.Driver}
    # Do not commit real credentials here.
    username: ${SPRING_DATASOURCE_USERNAME:}
    password: ${SPRING_DATASOURCE_PASSWORD:}

  jpa:
    database-platform: ${SPRING_JPA_DATABASE_PLATFORM:org.hibernate.dialect.MySQLDialect}
    hibernate:
      # Use update for local development. Prefer validate in production.
      ddl-auto: ${SPRING_JPA_HIBERNATE_DDL_AUTO:update}
    show-sql: ${SPRING_JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true

  h2:
    console:
      enabled: ${SPRING_H2_CONSOLE_ENABLED:true}
      path: /h2-console

  mail:
    host: ${SPRING_MAIL_HOST:smtp.qq.com}
    port: ${SPRING_MAIL_PORT:465}
    username: ${SPRING_MAIL_USERNAME:920267651@qq.com}
    password: ${SPRING_MAIL_PASSWORD:ssxamgalscgsbcbi}
    properties:
      mail:
        smtp:
          auth: ${SPRING_MAIL_SMTP_AUTH:true}
          ssl:
            enable: ${SPRING_MAIL_SMTP_SSL_ENABLE:true}
          ssl.trust: ${SPRING_MAIL_SMTP_SSL_TRUST:smtp.qq.com}
          starttls:
            enable: ${SPRING_MAIL_SMTP_STARTTLS_ENABLE:false}

billing:
  long-rent-threshold-hours: ${BILLING_LONG_RENT_THRESHOLD_HOURS:24}
  extra-long-rent-threshold-hours: ${BILLING_EXTRA_LONG_RENT_THRESHOLD_HOURS:72}
  long-rent-hour-rate-multiplier: ${BILLING_LONG_RENT_HOUR_RATE_MULTIPLIER:0.85}
  extra-long-rent-hour-rate-multiplier: ${BILLING_EXTRA_LONG_RENT_HOUR_RATE_MULTIPLIER:0.75}
  pending-payment-lock-minutes: ${BILLING_PENDING_PAYMENT_LOCK_MINUTES:5}

discount:
  enabled: ${DISCOUNT_ENABLED:true}
  rate: ${DISCOUNT_RATE:0.8}
  elderly-min-age: ${DISCOUNT_ELDERLY_MIN_AGE:60}
  rolling-days: ${DISCOUNT_ROLLING_DAYS:7}
  heavy-user-hours-threshold: ${DISCOUNT_HEAVY_USER_HOURS_THRESHOLD:8.0}

discount-verification:
  storage:
    root-dir: ${DISCOUNT_VERIFICATION_STORAGE_ROOT:uploads/discount-verifications}
    max-file-size-bytes: ${DISCOUNT_VERIFICATION_MAX_FILE_SIZE_BYTES:5242880}
    allowed-mime-types:
      - image/jpeg
      - image/png
      - application/pdf
