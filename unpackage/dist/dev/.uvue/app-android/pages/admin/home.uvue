import { ref, onMounted } from 'vue';
import { onShow } from '@dcloudio/uni-app';
import { BASE_URL } from '../../common/api.uts';
type LocationPoint = {
    __$originalPosition?: UTSSourceMapPosition<"LocationPoint", "pages/admin/home.uvue", 31, 6>;
    latitude: number;
    longitude: number;
};
const __sfc__ = defineComponent({
    __name: 'home',
    setup(__props) {
        const __ins = getCurrentInstance()!;
        const _ctx = __ins.proxy as InstanceType<typeof __sfc__>;
        const _cache = __ins.renderCache;
        const longitude = ref(116.397477);
        const latitude = ref(39.908692);
        const scale = ref(12);
        const markers = ref([] as Marker[]);
        const includePoints = ref([] as LocationPoint[]);
        const userLat = ref(null as number | null);
        const userLng = ref(null as number | null);
        const fetchScootersForMap = (myLat: number | null, myLng: number | null) => {
            uni.request({
                url: BASE_URL + '/api/v1/scooters',
                method: 'GET',
                success: (res) => {
                    if (res.statusCode != 200 || res.data == null) {
                        if (myLat != null && myLng != null) {
                            includePoints.value = [{ latitude: myLat, longitude: myLng } as LocationPoint];
                            longitude.value = myLng;
                            latitude.value = myLat;
                            scale.value = 16;
                        }
                        return;
                    }
                    const raw = res.data as UTSJSONObject;
                    let arr: UTSJSONObject[] = [];
                    if (raw['data'] != null) {
                        arr = raw['data'] as UTSJSONObject[];
                    }
                    else if (raw['scooters'] != null) {
                        arr = raw['scooters'] as UTSJSONObject[];
                    }
                    else if (raw['list'] != null) {
                        arr = raw['list'] as UTSJSONObject[];
                    }
                    const list: Marker[] = [];
                    const points: LocationPoint[] = [];
                    if (myLat != null && myLng != null) {
                        points.push({ latitude: myLat, longitude: myLng } as LocationPoint);
                    }
                    for (let i = 0; i < arr.length; i++) {
                        const item = arr[i] as UTSJSONObject;
                        const latVal = item['location_lat'] != null ? item['location_lat'] : item['locationLat'];
                        const lngVal = item['location_lng'] != null ? item['location_lng'] : item['locationLng'];
                        const numLat = latVal != null ? (latVal as number) : null;
                        const numLng = lngVal != null ? (lngVal as number) : null;
                        if (numLat != null && numLng != null && !isNaN(numLat) && !isNaN(numLng)) {
                            const rawId = item['id'];
                            const sid = rawId != null ? ('SC' + ('' + rawId)) : ('SC' + (i + 1));
                            list.push({
                                id: i + 1,
                                latitude: numLat,
                                longitude: numLng,
                                title: sid,
                                iconPath: '/static/location.png',
                                width: 32,
                                height: 32,
                                anchor: { x: 0.5, y: 1 } as Anchor
                            } as Marker);
                            points.push({ latitude: numLat, longitude: numLng } as LocationPoint);
                        }
                    }
                    markers.value = list;
                    includePoints.value = points;
                    if (points.length > 0) {
                        if (myLat != null && myLng != null) {
                            latitude.value = myLat;
                            longitude.value = myLng;
                        }
                        else {
                            let sumLat = 0.0;
                            let sumLng = 0.0;
                            for (let j = 0; j < points.length; j++) {
                                sumLat += points[j].latitude;
                                sumLng += points[j].longitude;
                            }
                            latitude.value = sumLat / points.length;
                            longitude.value = sumLng / points.length;
                        }
                        scale.value = points.length > 1 ? 14 : 16;
                    }
                }
            });
        };
        const loadMapWithLocation = () => {
            uni.getLocation({
                type: 'gcj02',
                success: (res) => {
                    const lng = res.longitude;
                    const lat = res.latitude;
                    userLat.value = lat;
                    userLng.value = lng;
                    longitude.value = lng;
                    latitude.value = lat;
                    fetchScootersForMap(lat, lng);
                },
                fail: () => {
                    fetchScootersForMap(null, null);
                }
            });
        };
        onMounted(() => {
            loadMapWithLocation();
        });
        onShow(() => {
            if (userLat.value != null && userLng.value != null) {
                fetchScootersForMap(userLat.value, userLng.value);
            }
            else {
                loadMapWithLocation();
            }
        });
        const goModifyScooter = () => {
            uni.navigateTo({
                url: '/pages/admin/modify'
            });
        };
        const goRevenue = () => {
            uni.navigateTo({
                url: '/pages/admin/revenue'
            });
        };
        const logout = () => {
            uni.showModal({
                title: 'Logout',
                content: 'Are you sure you want to logout?',
                success: (res) => {
                    if (res.confirm === true) {
                        uni.removeStorageSync('userInfo');
                        uni.removeStorageSync('token');
                        uni.removeStorageSync('userId');
                        uni.removeStorageSync('role');
                        uni.removeStorageSync('selectedScooterId');
                        uni.showToast({
                            title: 'Logged out successfully',
                            icon: 'success'
                        });
                        setTimeout(() => {
                            uni.reLaunch({
                                url: '/pages/login/login'
                            });
                        }, 800);
                    }
                }
            });
        };
        return (): any | null => {
            const _component_map = resolveComponent("map");
            return _cE("view", _uM({ class: "container" }), [
                _cE("view", _uM({ class: "title-section" }), [
                    _cE("text", _uM({ class: "title-text" }), "Admin Home"),
                    _cE("text", _uM({ class: "hint-text" }), "Manage scooters and revenue")
                ]),
                _cE("view", _uM({ class: "map-section" }), [
                    _cV(_component_map, _uM({
                        id: "adminMap",
                        class: "admin-map",
                        longitude: longitude.value,
                        latitude: latitude.value,
                        scale: scale.value,
                        markers: markers.value,
                        "include-points": includePoints.value,
                        "show-location": ""
                    }), null, 8 /* PROPS */, ["longitude", "latitude", "scale", "markers", "include-points"])
                ]),
                _cE("view", _uM({ class: "menu-section" }), [
                    _cE("button", _uM({
                        class: "menu-btn primary",
                        onClick: goModifyScooter
                    }), " Modify Scooter Information "),
                    _cE("button", _uM({
                        class: "menu-btn",
                        onClick: goRevenue
                    }), " View Revenue "),
                    _cE("button", _uM({
                        class: "menu-btn danger",
                        onClick: logout
                    }), " Logout ")
                ])
            ]);
        };
    }
});
export default __sfc__;
const GenPagesAdminHomeStyles = [_uM([["container", _pS(_uM([["display", "flex"], ["flexDirection", "column"], ["paddingTop", 24], ["paddingRight", 24], ["paddingBottom", 24], ["paddingLeft", 24], ["backgroundColor", "#f5f5f5"], ["minHeight", 750]]))], ["title-section", _pS(_uM([["display", "flex"], ["flexDirection", "column"], ["marginBottom", 16]]))], ["map-section", _pS(_uM([["width", "100%"], ["marginBottom", 20], ["borderTopLeftRadius", 8], ["borderTopRightRadius", 8], ["borderBottomRightRadius", 8], ["borderBottomLeftRadius", 8], ["overflow", "hidden"], ["boxShadow", "0 2px 8px rgba(0, 0, 0, 0.08)"]]))], ["admin-map", _pS(_uM([["width", "100%"], ["height", 280], ["backgroundColor", "#e5e5e5"]]))], ["title-text", _pS(_uM([["fontSize", 22], ["fontWeight", "bold"], ["color", "#333333"], ["marginBottom", 8]]))], ["hint-text", _pS(_uM([["fontSize", 14], ["color", "#666666"]]))], ["menu-section", _pS(_uM([["display", "flex"], ["flexDirection", "column"]]))], ["menu-btn", _uM([["", _uM([["paddingTop", 14], ["paddingRight", 14], ["paddingBottom", 14], ["paddingLeft", 14], ["borderTopLeftRadius", 8], ["borderTopRightRadius", 8], ["borderBottomRightRadius", 8], ["borderBottomLeftRadius", 8], ["borderTopWidth", 1], ["borderRightWidth", 1], ["borderBottomWidth", 1], ["borderLeftWidth", 1], ["borderTopStyle", "solid"], ["borderRightStyle", "solid"], ["borderBottomStyle", "solid"], ["borderLeftStyle", "solid"], ["borderTopColor", "#dddddd"], ["borderRightColor", "#dddddd"], ["borderBottomColor", "#dddddd"], ["borderLeftColor", "#dddddd"], ["fontSize", 16], ["fontWeight", "bold"], ["backgroundColor", "#ffffff"], ["color", "#333333"], ["boxShadow", "0 2px 8px rgba(0, 0, 0, 0.08)"], ["marginBottom", 24]])], [".primary", _uM([["backgroundColor", "#4CAF50"], ["borderTopColor", "#4CAF50"], ["borderRightColor", "#4CAF50"], ["borderBottomColor", "#4CAF50"], ["borderLeftColor", "#4CAF50"], ["color", "#ffffff"]])], [".danger", _uM([["backgroundColor", "#f44336"], ["borderTopColor", "#f44336"], ["borderRightColor", "#f44336"], ["borderBottomColor", "#f44336"], ["borderLeftColor", "#f44336"], ["color", "#ffffff"]])]])]])];
//# sourceMappingURL=home.uvue.map